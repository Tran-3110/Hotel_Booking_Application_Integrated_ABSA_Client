import { Inter, Open_Sans } from "next/font/google";

export const font = Inter({
    subsets: ["latin"],
    variable: "--font-inter",
});

export const fontLogo = Open_Sans({
    subsets: ["latin"],
    variable: "--font-open-sans",
})